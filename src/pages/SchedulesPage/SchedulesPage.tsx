import pageStyles from '../Pages.module.css';

const Schedules = () => {
    return (
        <div className={pageStyles.page}>
            Schedules
        </div>
    )
};

export default Schedules;