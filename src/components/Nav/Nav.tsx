import styles from './Nav.module.css';

const Nav = () => {
  return (
    <nav className={styles.nav}>
        <div className={styles.navContent}>
            skfjk
        </div>
    </nav>
  );
}

export default Nav;
